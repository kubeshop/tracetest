openapi: 3.0.0
components:
  schemas:

    Transaction:
      type: object
      properties:
        id:
          type: string
          readOnly: true
        name:
          type: string
        description:
          type: string
        version:
          type: integer
          description: version number of the test
        steps:
          type: array
          items:
            type: string
        createdAt:
          type: string
          format: date-time

    TransactionRun:
      type: object
      properties:
        id:
          type: string
          readOnly: true
        createdAt:
          type: string
          format: date-time
        state:
          type: string
          enum:
            [
              CREATED,
              EXECUTING,
              FINISHED,
              FAILED,
            ]
        environment:
          $ref: "./environments.yaml#/components/schemas/Environment"
