openapi: 3.0.0

components:
  schemas:
    MissingVariablesError:
      type: object
      properties:
        missingVariables:
          type: array
          items:
            $ref: "#/components/schemas/MissingVariable"
    MissingVariable:
      type: object
      properties:
        key:
          type: string
        defaultValue:
          type: string
