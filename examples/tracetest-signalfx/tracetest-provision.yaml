---
type: PollingProfile
spec:
  name: Default
  strategy: periodic
  default: true
  periodic:
    retryDelay: 5s
    timeout: 10m

---
type: DataStore
spec:
  name: SignalFX
  type: signalfx
  signalfx:
    token: <YOUR_TOKEN> # Add SignalFX token
    realm: us1 # Update this value if needed
