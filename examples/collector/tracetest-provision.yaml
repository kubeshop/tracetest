---
type: PollingProfile
spec:
  name: Default
  strategy: periodic
  default: true
  periodic:
    retryDelay: 5s
    timeout: 10m

---
type: Config
spec:
  analyticsEnabled: true

---
type: DataStore
spec:
  name: OpenTelemetry Collector
  type: otlp
  default: true
